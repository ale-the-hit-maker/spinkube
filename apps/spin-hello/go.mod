module hello-spin

go 1.21

// Pin the exact version with `go mod tidy` after scaffolding.
// The app can also be regenerated with: spin new -t http-go hello-spin
require github.com/fermyon/spin/sdk/go/v2 v2.2.0
